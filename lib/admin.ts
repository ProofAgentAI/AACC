// The single administrator account. Mirrors the is_admin() function in the
// database (supabase/schema-v7.sql) — change both together if it ever moves.
export const ADMIN_EMAIL = "contact@aacc-usa.org";
